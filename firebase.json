{
  "functions": {
    "source": "functions",
    "runtime": "nodejs18",
    "artifactRegistry": {
      "cleanupPolicyDays": 30
    },
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ]
  },
  "firestore": {
    "rules": "firestore.rules"
  }
}
